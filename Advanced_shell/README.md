# Advanced_shell

This folder contains exercises and small projects focused on advanced shell scripting and automation.

## Contents

- `Advanced_shell` — an empty file present in the folder (placeholder or script name collision).
- `apiAutomation-0x00/` — API automation exercises (folder).
- `batchProcessing-0x02/` — batch processing exercises or scripts (folder).
- `batchProcessing-0x04/` — additional batch processing exercises (folder).
- `data.json` — sample data used by one or more exercises.
- `data_extraction_automation-0x01/` — data extraction automation exercises (folder).
- `errors.txt` — captured errors or example error output.
- `pokemon_data_parallel/` — a small dataset of Pokémon JSON files for parallel processing examples.
  - contains: `bulbasaur.json`, `charmander.json`, `charmeleon.json`, `ivysaur.json`, `venusaur.json`
- `pokemon_report.csv` — CSV report likely generated from the Pokémon data.
- `summaryData-0x03/` — summary data and related exercises (folder).

## How to use

1. Inspect the individual subfolders to find scripts, sample inputs, and exercise descriptions.
2. Run scripts using `bash` or the shell specified in the scripts. Example:

```
cd Advanced_shell/apiAutomation-0x00
ls -la
```

3. If you find a missing README in a subfolder, I can create one summarizing its files as well.
