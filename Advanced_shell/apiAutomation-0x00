#!/bin/bash

# API URL
API_URL="https://pokeapi.co/api/v2/pokemon/pikachu"

# Output file
OUTPUT_FILE="data.json"
ERROR_FILE="errors.txt"

# Make request and handle errors
curl -s $API_URL -o $OUTPUT_FILE || echo "Failed to fetch data at $(date)" >> $ERROR_FILE
